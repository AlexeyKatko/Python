#Напишите функцию f, которая на вход принимает 
#два числа a и b, и возводит число a в целую 
#степень b с помощью рекурсии.

#Функция не должна ничего выводить, только возвращать значение.

a = int(input())
b = int(input())
def func(a, b):
    if b == 0:
        return 1
    else:
        return a*func(a, b-1)
 
print(func(a, b))
